{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "a": "lerna add",
    "b": "lerna bootstrap",
    "c": "lerna changed",
    "w": "yarn add --ignore-workspace-root-check --dev",
    "p": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.0.5"
  }
}
